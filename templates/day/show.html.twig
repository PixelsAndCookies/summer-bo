{% extends 'base.html.twig' %}

{% block title %}Day{% endblock %}

{% block body %}
    <h1>Day</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ day.id }}</td>
            </tr>
            <tr>
                <th>Id_day</th>
                <td>{{ day.idDay }}</td>
            </tr>
            <tr>
                <th>Template_day</th>
                <td>{{ day.templateDay }}</td>
            </tr>
            <tr>
                <th>Title_day</th>
                <td>{{ day.titleDay }}</td>
            </tr>
            <tr>
                <th>Date_day</th>
                <td>{{ day.dateDay ? day.dateDay|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('day.index') }}">back to list</a>

    <a href="{{ path('day.edit', {'id': day.id}) }}">edit</a>

    {{ include('day/_delete_form.html.twig') }}
{% endblock %}
