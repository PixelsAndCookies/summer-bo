{{ form_start(form) }}
    {% if picture.filename is defined %}
        <img src="/imgs/{{picture.filename}}" alt="">
    {% endif %}
    {{ form_widget(form) }}
    <button class="btn btn-primary mt-4">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
